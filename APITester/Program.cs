using System;
using System.IO;
using RowTorrentAPI;
using RowTorrentAPI.bencoding;

namespace APITester {
    class Program {
        static void Main(string[] args) {
//            var bstring = new BString("hello");
//            Console.WriteLine(bstring);
//
////            var element = new BElement<long>(12345);
////            MemoryStream stream = new MemoryStream();
////            var bnum = new BNumber(stream);
////            bnum.Encode(element);
////            foreach (var val in bnum.EncodedBElement.ValueBytes) {
////                Console.Write((char) val);
////            }
////            // Console.Write(bnum.EncodedBElement.ValueBytes);
//            Console.ReadKey();
        }
    }
}